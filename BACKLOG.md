# 氢璞 AI · 下一阶段功能 Backlog

> 更新时间：2026-08-21  
> 当前版本：前端重构完成，后端接口健康

---

## 一、可立即完善（代码层面）

### 1. 抽取统一 API 层
- **文件**：`frontend/src/lib/api.ts`
- **内容**：把所有 fetch 调用封装成函数，统一错误处理（401 自动跳转登录、403 提示权限不足、500 提示服务异常）
- **收益**：页面代码更简洁，错误处理一致

### 2. 抽取认证工具
- **文件**：`frontend/src/lib/auth.ts`
- **内容**：`getToken()`、`setAuth()`、`clearAuth()`、`isAdmin()`、`useAuth()` hook
- **收益**：避免 localStorage 操作散落在各处

### 3. 统一角色枚举
- **当前问题**：前端写 `customer_service/sales/technical`，后端校验 `customer_service/sales/technical_support`
- **修复**：前端 Select 的 `technical` 改为 `technical_support`，与后端一致

### 4. 答案区增强
- 复制答案按钮
- 显示生成耗时（后端返回 `elapsed_ms` 或前端计时）
- 显示引用角标（内联 `[1]` 点击跳转到来源卡片）

### 5. 文档详情增强
- JSON 下载按钮（下载 classified 原始数据）
- Markdown 导出（把产品大类/表格拼成 Markdown）
- 批量索引（多选文档一键向量化）

---

## 二、可改进（体验层面）

### 1. 错误边界
- 给每个页面加 `ErrorBoundary`，捕获渲染异常，显示友好提示而非白屏

### 2. 加载骨架屏
- 聊天答案、文档列表、管理看板在加载时显示骨架屏，而非转圈

### 3. 空状态引导
- 文档列表为空时，显示"上传第一份 PDF"的引导卡片
- 聊天历史为空时，显示角色说明和示例问题

### 4. 响应式适配
- 当前布局在 < 768px 侧栏会占满，需加折叠按钮
- 聊天输入框在移动端高度自适应

### 5. 键盘快捷键
- `Ctrl/Cmd + N`：新建对话
- `Ctrl/Cmd + K`：聚焦搜索/输入框
- `Esc`：关闭抽屉/弹窗

---

## 三、可新增功能（业务场景）

### 1. 方案配置器 ⭐⭐⭐
**场景**：客户选择应用场景 + 功率 + 部署方式 → 自动生成 BOM 与估算  
**实现**：
- 前端加一个"方案配置"入口（首页卡片或侧栏）
- 表单：场景（交通/能源/工业）、功率范围、部署方式（固定式/撬装/移动式）、氢气纯度要求
- 后端 `POST /api/v1/solution/configure`：根据配置匹配产品，返回推荐型号 + 参数对比表 + 估算价格区间
- 输出：可下载的 PDF 方案书

### 2. PDF 报价单生成 ⭐⭐⭐
**场景**：销售沟通后一键生成正式报价单  
**实现**：
- 在销售方案 DOCX 基础上，增加报价单模板
- 字段：客户信息、推荐型号、数量、单价、折扣、交付周期、有效期
- 后端用 `python-docx` 填充模板，返回 `.docx` 或 `.pdf`

### 3. 客户画像 ⭐⭐
**场景**：把会话与意向度合并为客户档案，跟踪复购  
**实现**：
- 新增 `customers` 表（user_id、意向度历史、标签、备注）
- 管理后台新增"客户"Tab，展示客户列表 + 详情
- 客户详情：意向度趋势图、历史会话、已下载方案

### 4. 多模态产品图问答 ⭐⭐
**场景**：上传实物/产品图直接识别型号  
**实现**：
- 聊天输入框支持图片上传（`multipart/form-data`）
- 后端调用 GPT-5.6 Luna 视觉模型识别图片中的产品型号
- 返回识别结果 + 对应产品页渲染图

### 5. 工单式人工介入 ⭐⭐
**场景**：客户点"联系人工"后自动开 Ticket  
**实现**：
- 聊天页加"联系人工"按钮
- 后端创建工单（`tickets` 表），通知销售（Slack/企微 webhook）
- 管理后台"工单"Tab，展示待处理/已处理工单

### 6. 销售 Playbook ⭐
**场景**：A 级线索自动派单 + SLA 提醒  
**实现**：
- 定时任务（Celery/APScheduler）扫描 lead_score >= 80 的会话
- 自动分配给在线销售，超过 24h 未跟进触发提醒
- 管理后台"派单"视图

### 7. 语音问答 ⭐
**场景**：现场工程师语音提问  
**实现**：
- 输入框加麦克风按钮，调用浏览器 `SpeechRecognition` ASR
- 答案用 `SpeechSynthesis` TTS 播报
- 可选：接入阿里云/腾讯云 ASR 服务提升准确率

### 8. 对比模式 ⭐
**场景**：客户一键比较两个型号  
**实现**：
- 聊天中识别"对比 A 和 B"意图
- 返回参数对比表格 + 适用场景差异
- 前端渲染为并排对比卡片

---

## 四、技术债务

1. **TypeScript 类型覆盖**：当前部分 `any` 类型，需补充 interface
2. **测试覆盖**：关键路径（登录、对话、上传）加 E2E 测试（Playwright）
3. **CI/CD**：GitHub Actions 自动构建 + 部署到测试环境
4. **监控**：接入 Sentry 前端错误追踪，后端 Prometheus 指标
5. **国际化**：当前硬编码中文，后续可加 i18n

---

## 五、推荐优先级

| 优先级 | 功能 | 理由 |
|--------|------|------|
| P0 | 统一 API 层 + 角色枚举修复 | 当前代码有错别字，影响技术角色 |
| P1 | 方案配置器 | 直接产出销售价值，黑客松亮点 |
| P1 | PDF 报价单 | 销售闭环的关键一环 |
| P2 | 客户画像 | 从工具升级为平台 |
| P2 | 工单式人工介入 | 提升客户体验 |
| P3 | 多模态产品图问答 | 技术展示价值高 |
| P3 | 语音问答 | 场景贴合但实现成本中等 |
| P3 | 对比模式 | 高频需求但可人工替代 |

---

## 六、安全提醒

- `.env` 中的 API Key 已在对话中暴露，**强烈建议轮换**
- 生产环境必须更换 `ADMIN_REGISTRATION_CODE`、JWT 密钥
- 建议给 `/admin` 接口加 IP 白名单或 VPN 限制
- 文件上传需限制大小和类型，防止恶意文件

---

*文档结束*